spring:
  profiles:
    active: dev
  datasource:
#    url: jdbc:mysql://35.221.217.3:3306/furseal?createDatabaseIfNotExist=true
    url: jdbc:mysql://localhost:3306/furseal?createDatabaseIfNotExist=true
    username: root
    password: Kyo77437743
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 9000

keycloak:
  jwk: ${keycloak.url}/auth/realms/${keycloak.realm}/protocol/openid-connect/certs
  url: http://localhost:8080
  realm: test

#keycloak:
#  url: http://localhost:8080
#  realm: test

management:
  metrics:
    tags:
      application: movies-backend

my:
  message: dev-message
